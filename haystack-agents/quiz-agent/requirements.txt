# Fetch.ai agent framework (pins proven by langchain-agents/deep-agents/hackflow-agent)
uagents==0.23.6
uagents-core==0.4.0

# Haystack AI orchestration framework (indexing + RAG + question generation)
haystack-ai>=2.30.0

# Haystack converter / fetcher backends
trafilatura>=1.6.0        # backend for HTMLToDocument
pypdf>=4.0.0              # backend for PyPDFToDocument

# OpenAI client — required by OpenAIChatGenerator (pointed at ASI:One, not OpenAI)
openai>=1.30.0

# Stripe embedded Checkout — the rail behind the Fetch payment protocol
stripe>=8.0.0

# Utilities
python-dotenv>=1.0.0
nltk>=3.9.1               # sentence tokenizer used by Haystack DocumentSplitter
requests>=2.31.0          # downloads PDFs from Agentverse External Storage
brotlicffi>=1.0.9.2       # Brotli decompression for aiohttp (mailbox) + requests (URL fetching); Python 3.13-compatible replacement for the old 'brotli' package
