uagents==0.23.6
uagents-core==0.4.0
pydantic>=2.0,<3
python-dotenv>=1.0
certifi
requests>=2.31

# Layer 2: PDF / DOCX parsing
pypdf>=4.0
python-docx>=1.1

# Layer 3: vector store + embeddings (FastEmbed bundles a small ONNX model)
qdrant-client[fastembed]>=1.9

# LLM (ASI:One via OpenAI-compatible client)
openai>=1.30
